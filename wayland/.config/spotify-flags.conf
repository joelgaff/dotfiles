# Same reasoning as the *-flags.conf files beside this one: keep the client off
# XWayland so fractional scaling stays sharp. Spotify embeds CEF rather than
# Electron and takes the explicit switch; it has no --ozone-platform-hint.
--ozone-platform=wayland
