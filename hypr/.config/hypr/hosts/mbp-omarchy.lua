-- MacBook Pro running Omarchy.
-- Values carried over from the pre-Quattro monitors.conf. That machine was
-- still on Omarchy 3.8.5 when this port was written, so these are untested
-- under Quattro; check them when you upgrade it.

hl.monitor({ output = "eDP-1", mode = "2880x1800@60", position = "0x0", scale = 2 })
